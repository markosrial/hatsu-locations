spring:
  ## Database properties ##
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 1
